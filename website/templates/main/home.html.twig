{% extends 'base.html.twig' %}

{% set menuCurrentItem="home" %}

{% block header_title %}{{  parent() }} - {{ 'app.page.home.title'|trans }}{% endblock %}

{% block main_title %}{{ 'app.page.home.title'|trans }}{% endblock %}

{% block body %}
    <div class="row">
        <div class="col-12">
            <div class="card">
                <div class="card-header">
                    Last Log
                </div>
                <div class="card-body">
                    <pre>{{ content }}</pre>
                </div>
            </div>
        </div>
    </div>
{% endblock body %}
